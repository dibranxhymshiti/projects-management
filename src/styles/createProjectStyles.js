import { makeStyles } from "@material-ui/core";

const createProjectStyles = makeStyles((theme) => ({
  textField: {
    marginTop: 10,
    marginBottom: 10,
    width: "100%",
  },
}));

export default createProjectStyles;
